/**
 * Tailwind CSS configuration for News-Mania frontend.
 * Purpose: Defines Tailwind settings, content paths, and theme customizations.
 * Used for styling the application with utility-first CSS classes.
 */

/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
}

